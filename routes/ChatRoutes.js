const express = require("express")
const ChatController = require("../controllers/ChatController")
const router = express.Router()
// criar o endpoint  para a criação do chat

router.

module.exports = router